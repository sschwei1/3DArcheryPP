import React, { useState } from 'react'

const PlayEvent = () => {
  const [authCode, setAuthCode] = useState();

  return (
    // authCode ? 
    //   <AuthView /> :
    //   <GameView />
    <></>
  )
}

export default PlayEvent
